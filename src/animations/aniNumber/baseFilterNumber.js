import BaseFilter from "../animationDesign/baseFilter.js";
export default class BaseFilterNumber extends BaseFilter {
    constructor(startTimeSec, endTimeSec) {
        super(startTimeSec, endTimeSec);
    }
}

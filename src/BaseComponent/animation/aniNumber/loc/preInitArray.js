export default class PreInitArray {
    constructor(timeFrom, timeTo, from, to) {
        this.from = from;
        this.to = to;
        this.timeFrom = timeFrom;
        this.timeTo = timeTo;
    }
}

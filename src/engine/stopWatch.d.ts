export default class StopWatch {
    private _stopFlag;
    private runningStartTimeTS;
    constructor();
    isRunning(): boolean;
    shouldStop(): boolean;
    stop(): boolean;
    start(): boolean;
    getMsDelta(): number;
}
//# sourceMappingURL=stopWatch.d.ts.map
export default class PreInitVibrate {
    constructor(from, to, seed, offset, delay) {
        this.from = from;
        this.to = to;
        this.seed = seed;
        this.offset = offset;
        this.delay = delay;
    }
}
